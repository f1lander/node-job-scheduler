import { Router } from "express";
// import { register, login } from "../controllers/users";

const ReportRouter = Router();

// ReportRouter.post("/register", register);
// ReportRouter.post("/authenticate", login);

export default ReportRouter;
